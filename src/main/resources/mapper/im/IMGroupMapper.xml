<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hsx.manyue.modules.im.mapper.IMGroupMapper">

    <select id="queryUserGroups" resultType="com.hsx.manyue.modules.im.model.entity.IMGroupEntity">
        SELECT g.* FROM t_im_group g
        INNER JOIN t_im_group_member gm ON g.id = gm.group_id
        WHERE gm.user_id = #{userId}
        AND g.status = 1
        AND g.is_delete = 0
        AND gm.is_delete = 0
    </select>

</mapper>
